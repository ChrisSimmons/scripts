#!/usr/bin/env bash
# git configuration for Chris
# $ source <(curl -s https://raw.githubusercontent.com/ChrisSimmons/system-bootstrapping/main/git-config-chris.sh)

git config --global user.email "chris.dev@netchris.com"
git config --global user.name "Chris Simmons"
