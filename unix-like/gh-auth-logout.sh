#!/usr/bin/env bash
# GitHub CLI logout (for cleanup after pushing up SSH key)
# $ source <(curl -s https://raw.githubusercontent.com/ChrisSimmons/scripts/main/unix-like/gh-auth-logout.sh)

gh auth logout
